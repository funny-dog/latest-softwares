# 生产依赖 —— 精确 pin 提高 pip 可复现性。
# 升级流程：编辑此处版本号 → 本地 pip install -r 验证 → 跑 pytest → 提交 PR。
# 配合 CI 全套测试验证，避免上游隐式 minor 升级带来的回归。
fastapi[standard]==0.136.1
PyYAML==6.0.3
requests==2.33.1
Jinja2==3.1.6
# 远程统计持久化:serverless 上本地 SQLite 不持久,改写远程 Turso/libSQL。
# 仅在配置了 TURSO_DATABASE_URL 时(生产部署)才会被导入使用。
libsql==0.1.11
